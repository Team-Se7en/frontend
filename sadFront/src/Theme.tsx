import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#5899E2',
        },
        secondary: {
            main: '#14213D',
        },
    },
});

export default theme;